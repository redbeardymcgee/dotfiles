[edit]
apply = true
watch = true

[git]
autoCommit = true
autoPush = true

progress = true
scriptTempDir = "{{ .chezmoi.fqdnHostname }}"
{{ if hasPrefix "dev" .chezmoi.fqdnHostname }}
sourceDir = "{{ .chezmoi.homeDir }}/../.local/share/chezmoi"
{{ end }}

umask = 0o22
