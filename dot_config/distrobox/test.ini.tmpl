[test]
image="registry.fedoraproject.org/fedora-toolbox:latest"
pull=true
start_now=true

home={{ .testbox }}
entry=false

additional_flags='--hostname "dev.{{ .chezmoi.hostname }}"'

#pre_init_hooks='sudo -u {{ .chezmoi.username }} ln -sf {{ .chezmoi.homeDir }}/.ssh {{ .testbox }}'
init_hooks='sh -c \"\$(curl -fsLS get.chezmoi.io)\"'
#-- -b "$HOME/testbox/.local/bin"'
#init {{ .testboxUser }} --apply --ssh'
init_hooks='echo devbox.bash'

# home=""
# pre_init_hooks=""
# volume=""
# init=
# root=
# unshare_ipc=
# unshare_netns=
# unshare_process=
# unshare_devsys=
# unshare_all=
