[test]
image="registry.fedoraproject.org/fedora-toolbox:latest"
pull=true
start_now=true

#home={{ .testbox }}
entry=false

additional_flags='--hostname "test.{{ .chezmoi.hostname }}"'

pre_init_hooks='ln -sf {{ .chezmoi.homeDir }}/.ssh {{ .testbox }}.ssh;'
init_hooks='chezmoi init redbeardymcgee --apply --promptDefaults --keep-going --force;'

#init_hooks='echo devbox.bash'

# home=""
# pre_init_hooks=""
# volume=""
# init=
# root=
# unshare_ipc=
# unshare_netns=
# unshare_process=
# unshare_devsys=
# unshare_all=
