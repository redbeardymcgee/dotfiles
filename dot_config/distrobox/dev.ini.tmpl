[dev]
image="registry.fedoraproject.org/fedora-toolbox:latest"
pull=true
start_now=true

{{ if eq .chezmoi.hostname "rbmpc" }}
nvidia=true
{{ end }}

home={{ .devbox }}
entry=false

additional_flags="--hostname 'dev.{{ .chezmoi.hostname }}'"

#pre_init_hooks='sudo -u {{ .chezmoi.username }} ln -sf {{ .chezmoi.homeDir }}/.ssh {{ .devbox }};'
#init_hooks='sudo -u {{ .chezmoi.username }} sh -c {{ .curlChezmoi }} -- -b {{ .devboxBin }} init {{ .devboxUser }} --apply --force --keep-going --promptDefaults;'
#init_hooks='devbox.bash;'

# home=""
# pre_init_hooks=""
# volume=""
# init=
# root=
# unshare_ipc=
# unshare_netns=
# unshare_process=
# unshare_devsys=
# unshare_all=
